name: My Actions Test

on: workflow_dispatch

jobs:
  job1:
    name: test-job1
    runs-on: ubuntu-18.04
    steps:
      - name: Hello
        env:
          TEST_ENV: World!
          Auther: PP
          FOLDER: ./
          BUILD_SCRIPT: $TEST_ENV >> buildtest.ini
    
